services:
  news-brief:
    build: .
    image: personal-news-intelligence:phase1
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./data:/app/data
      - ./config:/app/config:ro
    command: ["run"]
